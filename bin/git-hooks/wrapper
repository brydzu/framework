#!/bin/bash
set -e

. .git/hooks/env

if [ -x $0.local ]; then
    $0.local "$@"
fi
if [ -d "bin/git-hooks/$(basename $0)" ]; then
    for SCRIPT in bin/git-hooks/$(basename $0)/*; do
        [ -x "$SCRIPT" ] || continue
        "$SCRIPT" "$@"
    done
fi
